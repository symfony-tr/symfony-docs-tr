expanded
~~~~~~~~

**tip**: ``Boolean`` **varsayılan**: ``false``

If set to true, radio buttons or checkboxes will be rendered (depending
on the ``multiple`` value). If false, a select element will be rendered.

Eğer true yapılırsa radio butonları ya da checkbox'lar ekrana basılır
(``multiple`` seçeneğinin aldığı değere göre). Eğer false ise bir
select elementi ekrana basılacaktır.
