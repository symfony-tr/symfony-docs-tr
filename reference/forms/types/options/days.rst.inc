days
~~~~

**tip**: ``array`` **varsayılan**: 1 'den 31'e kadar.

Gün alan tipinde kullanılacak günlerin sayısal değerlerinin listesidir.
Bu seçenek sadece ``widget`` seçeneğinin ``choice`` olduğu durumda çalışır::
 

    'days' => range(1,31)
